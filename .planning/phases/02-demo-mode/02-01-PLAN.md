---
phase: 02-demo-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/store/demo-store.ts
  - apps/web/src/store/demo-store.test.ts
  - packages/ui/src/components/input/ExemplarPanel.tsx
  - packages/ui/src/index.ts
  - apps/web/src/App.tsx
autonomous: true
requirements:
  - PHASE-02-SC-01  # Demo button exists and toggles exemplarMode
  - PHASE-02-SC-03  # Toggling is reversible and session-scoped (no persist)
  - PHASE-02-SC-07  # Subtle "Demo" chip per card while demo mode active

must_haves:
  truths:
    - "A 'Demo' button is visible in the exemplar panel header on the input screen."
    - "Clicking the Demo button toggles a session-scoped store flag exemplarMode between 'lean' and 'demo'."
    - "While exemplarMode === 'demo', each exemplar card renders a small 'Demo' chip badge."
    - "Refreshing the browser returns the app to exemplarMode === 'lean' (no localStorage persistence of the flag)."
  artifacts:
    - path: "apps/web/src/store/demo-store.ts"
      provides: "Zustand store exposing exemplarMode flag + toggleExemplarMode/setExemplarMode actions, NOT wrapped in persist middleware"
      exports: ["useDemoStore", "ExemplarMode"]
    - path: "apps/web/src/store/demo-store.test.ts"
      provides: "Unit tests for default mode, toggle, set, and that no localStorage write occurs"
    - path: "packages/ui/src/components/input/ExemplarPanel.tsx"
      provides: "Updated ExemplarPanel that accepts exemplarMode + onToggleMode props, renders the Demo button, and shows a 'Demo' chip on each card when mode === 'demo'"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/store/demo-store.ts"
      via: "useDemoStore() reads exemplarMode and toggleExemplarMode, passes both into <ExemplarPanel />"
      pattern: "useDemoStore|exemplarMode"
    - from: "packages/ui/src/components/input/ExemplarPanel.tsx"
      to: "Demo button + per-card chip"
      via: "conditional render keyed off the exemplarMode prop"
      pattern: "exemplarMode\\s*===\\s*'demo'"
---

<objective>
Add the smallest demonstrable Demo Mode slice: a session-scoped Zustand store flag (`exemplarMode: 'lean' | 'demo'`), a Demo toggle button rendered in the existing exemplar panel header, and a subtle "Demo" chip on each exemplar card while in demo mode. This plan does NOT yet wire the actual demo-payload load path — clicking a card while in demo mode is allowed to fall back to the existing lean behavior. Plan 02-03 will reroute the click.

Purpose: Land the visible toggle UI and store contract first so Plan 02-03 has a stable interface to consume, and so the affordance is reviewable visually before the data plumbing lands.

Output: A new `demo-store.ts` (no persist middleware), updated `ExemplarPanel.tsx` with a Demo button + per-card chip, and a wired-up `App.tsx` reading the store and passing props down.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-demo-mode/02-CONTEXT.md
@apps/web/src/store/input-store.ts
@packages/ui/src/components/input/ExemplarPanel.tsx

<interfaces>
<!-- Existing ExemplarPanel public surface (extracted from current source). The new -->
<!-- props (exemplarMode, onToggleMode) are additive — keep onSelect/exemplars/etc. -->

From packages/ui/src/components/input/ExemplarPanel.tsx (current):
```typescript
interface ExemplarItem {
  id: string;
  label: string;
  description: string;
}

interface ExemplarPanelProps {
  exemplars: ExemplarItem[];
  isLoading: boolean;
  loadingId: string | null;
  onSelect: (id: string) => void;
}
```

Existing Zustand store reference (do NOT copy `persist` middleware — demo-store must be session-scoped):
- `apps/web/src/store/input-store.ts` uses `persist(...)` with `createDebouncedStorage()`. The demo store deliberately does NOT use persist.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create demo-store with session-scoped exemplarMode flag</name>
  <files>apps/web/src/store/demo-store.ts, apps/web/src/store/demo-store.test.ts</files>
  <behavior>
    - Default state: exemplarMode === 'lean' on every fresh store creation.
    - toggleExemplarMode(): flips lean ↔ demo.
    - setExemplarMode('demo' | 'lean'): sets explicitly.
    - No localStorage key is written when toggle/set runs (session-scoped per D from CONTEXT.md "Mode flag is session-scoped, NOT persisted"). Verify by snapshotting localStorage keys before/after toggling.
  </behavior>
  <action>
    Create `apps/web/src/store/demo-store.ts` exporting:
      - `export type ExemplarMode = 'lean' | 'demo';`
      - `export const useDemoStore = create<DemoState>(...)` — plain Zustand `create`, NO `persist` middleware import. Per the locked decision in 02-CONTEXT.md: "Mode is a Zustand store flag ... Session-scoped — does NOT persist via the existing persist middleware. Resets to 'lean' on every app load."
      - State shape: `{ exemplarMode: ExemplarMode; toggleExemplarMode: () => void; setExemplarMode: (mode: ExemplarMode) => void }`.

    Create `apps/web/src/store/demo-store.test.ts` mirroring the style of `apps/web/src/store/input-store.test.ts`:
      - `it('defaults to lean')`
      - `it('toggleExemplarMode flips lean to demo and back')`
      - `it('setExemplarMode sets explicitly')`
      - `it('does NOT write to localStorage when toggled')` — capture `Object.keys(localStorage)` before and after `toggleExemplarMode()`; assert no new key matching `/demo/i` appears.

    Reset the store between tests via `useDemoStore.setState({ exemplarMode: 'lean' })` in `beforeEach`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run src/store/demo-store.test.ts</automated>
  </verify>
  <done>All 4 unit tests pass; `demo-store.ts` exports `useDemoStore` and `ExemplarMode`; grep confirms NO `persist` import in `demo-store.ts`: `grep -c "from 'zustand/middleware'" apps/web/src/store/demo-store.ts` returns `0`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend ExemplarPanel with Demo toggle button + per-card chip</name>
  <files>packages/ui/src/components/input/ExemplarPanel.tsx, packages/ui/src/index.ts</files>
  <behavior>
    - Component accepts new props `exemplarMode: 'lean' | 'demo'` and `onToggleMode: () => void`.
    - When `exemplarMode === 'lean'`: button reads "Demo", styled as inactive (outlined). No chips.
    - When `exemplarMode === 'demo'`: button reads "Demo" but styled as active (filled accent). Each card renders a small "Demo" chip pill in its top-right corner (`absolute top-1 right-1` style; pill background visibly distinct from card surface).
    - Chip is a `<span>` with `data-testid="demo-chip"` so tests can count them deterministically.
    - Button has `data-testid="demo-toggle"` and `aria-pressed={exemplarMode === 'demo'}`.
    - Existing onSelect / exemplars / isLoading / loadingId props remain unchanged in shape and behavior.
  </behavior>
  <action>
    Edit `ExemplarPanel.tsx`:
      1. Extend `ExemplarPanelProps` with `exemplarMode: 'lean' | 'demo'` and `onToggleMode: () => void`.
      2. In the panel header row (the existing flex with the icon + "Try an Exemplar" text), add a right-aligned `<button>` with the data-testid + aria-pressed attributes above. Use Tailwind classes consistent with the existing teal palette (e.g., active = `bg-teal-600 text-white`, inactive = `border border-teal-300 text-teal-700 bg-white`). Keep it small (`text-xs px-2 py-1 rounded-md`).
      3. On each card button, change the wrapping element from `<button>` to `<button className="relative ...">` and conditionally render `{exemplarMode === 'demo' && <span data-testid="demo-chip" className="absolute top-1 right-1 ...">Demo</span>}` inside.
      4. Make the chip subtle but visible — small uppercase text, accent background. Pick concrete classes (e.g., `text-[9px] uppercase tracking-wider font-bold bg-teal-600 text-white rounded-sm px-1`). Consistent with project's existing `text-[11px] font-bold uppercase tracking-wider` pattern.

    Re-export remains unchanged in `packages/ui/src/index.ts` (already exports `ExemplarPanel`); add `export type { ExemplarPanelProps } from './components/input/ExemplarPanel';` only if not already present.

    No new test file required for this task — Task 3's wiring test exercises rendering. Type-check via `pnpm -w typecheck` is the gate.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-mapper" && pnpm -w typecheck 2>&1 | tail -5 && grep -c "data-testid=\"demo-toggle\"" packages/ui/src/components/input/ExemplarPanel.tsx</automated>
  </verify>
  <done>Typecheck passes; `grep -c data-testid=\"demo-toggle\" packages/ui/src/components/input/ExemplarPanel.tsx` ≥ 1; `grep -c data-testid=\"demo-chip\" packages/ui/src/components/input/ExemplarPanel.tsx` ≥ 1.</done>
</task>

<task type="auto">
  <name>Task 3: Wire demo-store into App.tsx and pass props to ExemplarPanel</name>
  <files>apps/web/src/App.tsx</files>
  <action>
    In `apps/web/src/App.tsx`:
      1. Add `import { useDemoStore } from './store/demo-store';` near the other store imports.
      2. Inside the `App` component body, add: `const exemplarMode = useDemoStore((s) => s.exemplarMode);` and `const toggleExemplarMode = useDemoStore((s) => s.toggleExemplarMode);`.
      3. Update the `<ExemplarPanel ... />` JSX (around line 960) to pass the new props:
         ```tsx
         <ExemplarPanel
           exemplars={EXEMPLARS}
           isLoading={loadingExemplarId !== null}
           loadingId={loadingExemplarId}
           onSelect={handleExemplarSelect}
           exemplarMode={exemplarMode}
           onToggleMode={toggleExemplarMode}
         />
         ```
      4. Do NOT change `handleExemplarSelect` in this plan — Plan 02-03 owns rerouting based on `exemplarMode`.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-mapper" && pnpm -w typecheck 2>&1 | tail -5 && grep -c "useDemoStore" apps/web/src/App.tsx</automated>
  </verify>
  <done>Typecheck passes; `grep -c useDemoStore apps/web/src/App.tsx` ≥ 2 (import + usage); `grep -c exemplarMode={exemplarMode} apps/web/src/App.tsx` === 1.</done>
</task>

</tasks>

<threat_model>
No new attack surface in this plan. The store flag is in-memory-only and never serialized to disk or transmitted. UI changes are pure render-side.
</threat_model>

<verification>
- All unit tests pass: `cd apps/web && pnpm vitest run src/store/demo-store.test.ts`
- Workspace typechecks clean: `pnpm -w typecheck`
- Manual smoke (optional, not gating): `pnpm dev` → input screen → Demo button visible in exemplar panel header → click → chip appears on each of 10 cards → click again → chips disappear → refresh page → mode is back to lean.
</verification>

<success_criteria>
Backed by phase success criteria #1, #3, #7:
- [x] Demo button exists on exemplar carousel surface and toggles exemplarMode (#1).
- [x] Toggling is reversible AND session-scoped — refresh returns to lean (#3); verified by absence of `persist` middleware import.
- [x] Subtle "Demo" chip on each exemplar card when demo mode active (#7).
</success_criteria>

<output>
After completion, create `.planning/phases/02-demo-mode/02-01-SUMMARY.md`.
</output>
