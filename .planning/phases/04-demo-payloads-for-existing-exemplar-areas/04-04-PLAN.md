---
phase: 04-demo-payloads-for-existing-exemplar-areas
plan: 04
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - scripts/demos/corporate-ma.input.json
  - scripts/demos/ip-tech.input.json
  - scripts/demos/commercial-lit.input.json
  - apps/web/src/exemplar/demos/corporate-ma.demo.json
  - apps/web/src/exemplar/demos/ip-tech.demo.json
  - apps/web/src/exemplar/demos/commercial-lit.demo.json
  - apps/web/src/__tests__/demo-mode-roundtrip.test.tsx
  - apps/web/src/__tests__/demo-mode-no-network.test.tsx
  - apps/web/src/__tests__/demo-mode-richness.test.ts
autonomous: false
requirements: [DEMO-01, DEMO-02, DEMO-03, DEMO-04, DEMO-05]
user_setup:
  - service: anthropic
    why: "D-04 LLM curation with claude-3-5-sonnet-latest; executor has no API key/live backend in CI"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys (operator's own key)"
    dashboard_config:
      - task: "Start backend: pnpm dev:api (port 58000) before curating"
        location: "local shell"
must_haves:
  truths:
    - "corporate-ma, ip-tech, and commercial-lit each have an input.json (lean verbatim + thematically placed enrichments) and an anthropic-curated demo.json"
    - "Each demo.json round-trips identically to a drag-drop session and shows a visible auto-accept/pending mix (0 < completed < total_nodes)"
    - "Zero LLM/pipeline network calls occur when any of these demos loads at runtime"
  artifacts:
    - path: "apps/web/src/exemplar/demos/corporate-ma.demo.json"
      provides: "Curated corporate-ma demo session (SessionFile v1.3, provider anthropic)"
      contains: "anthropic"
    - path: "scripts/demos/ip-tech.input.json"
      provides: "Curation input for ip-tech"
      contains: "enrichments"
  key_links:
    - from: "apps/web/src/__tests__/demo-mode-richness.test.ts"
      to: "the 3 new demo.json files"
      via: "it.each rows asserting 0 < completed < total_nodes"
      pattern: "toBeLessThan"
---

<objective>
Batch 2 of the per-area curation loop: produce demo payloads for the transactional/litigation areas corporate-ma, ip-tech, and commercial-lit. Same loop as Plan 03 (probe then author input.json then operator LLM curation at tuned threshold then verify richness then register + test rows), minus the PI re-curation. Per RESEARCH.md Pitfall 6, these areas may yield fewer intuitive high-coverage enrichments; coherence outranks richness (D-02).

Purpose: Second installment of the 9 deferred-area demos (DEMO-01..05).

Output: 3 input.json + 3 demo.json + test rows for all 3 in the three harnesses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-CONTEXT.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-RESEARCH.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md
@scripts/demos/personal-injury.input.json
@scripts/curate_demos.py

<interfaces>
<!-- Curation CLI: backend/.venv/bin/python scripts/curate_demos.py --area {slug} --provider anthropic --threshold 0.85 --backend http://127.0.0.1:58000 -->
<!-- input.json shape: slug, label, text (lean verbatim + enrichments inserted thematically as tab-tab leaves), enrichments[], source_lean_exemplar, spike_reference. curate reads ONLY text. -->
<!-- demo.json required: version "1.3", provider "anthropic", model "claude-3-5-sonnet-latest", screen "mapping", selections (string-keyed), node_statuses, pipeline_version, folio_version, total_nodes, completed -->
<!-- Slugs this batch: corporate-ma, ip-tech, commercial-lit -->
<!-- Test harness tables already hold PI + batch-1 rows; append batch-2 rows at the marked insertion points. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Operator runs probe + LLM curation (batch 2)</name>
  <what-built>Probe + curation tooling ready (Plans 01-02). The LLM curation requires the operator's ANTHROPIC_API_KEY and a running backend; the executor cannot run it.</what-built>
  <action>Operator step (cannot be executor-automated): with backend running (pnpm dev:api, port 58000) and ANTHROPIC_API_KEY exported, run run_probe.py --area {slug} then curate_demos.py --area {slug} --provider anthropic --threshold 0.85 for corporate-ma, ip-tech, and commercial-lit (input.json authored in Task 1 first). Tune --threshold per area for a visible mix (0.55-0.80). Do not commit the API key.</action>
  <decision>Operator runs the probe + curation for corporate-ma, ip-tech, commercial-lit.</decision>
  <how-to-verify>
    1. Backend running: pnpm dev:api (port 58000). 2. export ANTHROPIC_API_KEY=sk-ant-...
    3. For each slug in {corporate-ma, ip-tech, commercial-lit}: after its input.json exists, run run_probe.py --area {slug} then curate_demos.py --area {slug} --provider anthropic --threshold 0.85.
    4. Read stderr "Auto-mapped: X (unmapped: Y)". Target X over (X+Y) in 0.55-0.80. All-accepted means raise to 0.90/0.92; all-pending means lower to 0.75/0.80.
  </how-to-verify>
  <resume-signal>Type "curated" once all 3 demo.json exist with provider anthropic, or describe blockers.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Author input.json for corporate-ma, ip-tech, commercial-lit</name>
  <files>scripts/demos/corporate-ma.input.json, scripts/demos/ip-tech.input.json, scripts/demos/commercial-lit.input.json</files>
  <read_first>
    - scripts/demos/personal-injury.input.json (analog shape; enrichments placed under thematically relevant branch)
    - scripts/demos/{slug}-probe-candidates.json for the 3 slugs (operator probe output: pick enrichments with high_score_relevant >= 4 in coherent branches; avoid trap terms)
    - .planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md ("Verbatim lean texts per slug" table + input.json pattern)
  </read_first>
  <action>
    For each of corporate-ma, ip-tech, commercial-lit: create scripts/demos/{slug}.input.json matching the PI analog. Set slug, label (carousel label from RESEARCH.md slug table: Corporate M&A, IP & Technology, Commercial Litigation), source_lean_exemplar = packages/core/src/exemplar/data.ts#{slug}, spike_reference = .planning/spikes/001-demo-pi-curation/README.md. Build text from the verbatim lean hierarchy (root + 5 branches + 10 leaves, tab-indented as in data.ts, joined with newlines), inserting 2-3 enrichment leaves as tab-tab lines under the thematically appropriate branch. Choose enrichments from probe output where high_score_relevant >= 4 in a coherent branch. Per Pitfall 6 / D-02: if a transactional area yields only 1-2 coherent enrichments, ship that many rather than forcing off-domain noise, and note the shortfall in the SUMMARY. Record chosen labels in enrichments[].
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-mapper" && for s in corporate-ma ip-tech commercial-lit; do python3 -m json.tool "scripts/demos/$s.input.json" > /dev/null || exit 1; done && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 3 input.json files are valid JSON with slug, label, text, enrichments, source_lean_exemplar, spike_reference.
    - text begins with the verbatim root line and contains all 5 branch + 10 leaf labels from the PATTERNS.md table for each slug, plus enrichment leaves under thematically relevant branches.
    - enrichments[] lists exactly the enrichment labels inserted into text.
  </acceptance_criteria>
  <done>Three input.json authored; enrichments chosen from probe output, placed thematically.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the 3 curated demo.json + extend test tables</name>
  <files>apps/web/src/exemplar/demos/corporate-ma.demo.json, apps/web/src/exemplar/demos/ip-tech.demo.json, apps/web/src/exemplar/demos/commercial-lit.demo.json, apps/web/src/__tests__/demo-mode-roundtrip.test.tsx, apps/web/src/__tests__/demo-mode-no-network.test.tsx, apps/web/src/__tests__/demo-mode-richness.test.ts</files>
  <read_first>
    - apps/web/src/__tests__/demo-mode-roundtrip.test.tsx (it.each table with PI + batch-1 rows; append here)
    - apps/web/src/__tests__/demo-mode-no-network.test.tsx (it.each table; append)
    - apps/web/src/__tests__/demo-mode-richness.test.ts (it.each table; append)
    - apps/web/src/exemplar/demos/index.ts (confirm corporate-ma/ip-tech/commercial-lit in LAZY_LOADERS + DEMO_AVAILABLE_SLUGS; wired in Plan 01)
    - .planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md (test extension patterns + Provider/Model verification one-liner)
  </read_first>
  <action>
    Precondition: operator produced all 3 demo.json (checkpoint above). Validate each: provider equals anthropic, model equals claude-3-5-sonnet-latest, version equals 1.3, and 0 < completed < total_nodes (re-tune + re-curate if all-accepted or all-pending). LAZY_LOADERS for these slugs were wired in Plan 01 so no index.ts edit is needed; just confirm presence. Extend the three test harnesses: add a static import for each of the 3 demo.json and append a table row in roundtrip, no-network, and richness it.each tables. Run the full demo test set to confirm all rows (PI + batch 1 + batch 2 = 7 areas) are green.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-mapper" && pnpm --filter @folio-mapper/web test --run src/__tests__/demo-mode-roundtrip.test.tsx src/__tests__/demo-mode-no-network.test.tsx src/__tests__/demo-mode-richness.test.ts src/exemplar/demos/index.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 3 demo.json: provider anthropic, model claude-3-5-sonnet-latest, version 1.3, 0 < completed < total_nodes.
    - roundtrip, no-network, and richness test files each contain rows for all 7 covered slugs so far.
    - Full demo test set runs green (no failures).
    - DEMO_AVAILABLE_SLUGS still lists all 10 slugs.
  </acceptance_criteria>
  <done>3 demo payloads validated, registered (via Plan 01 loaders), and green across round-trip, no-network, and richness tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> Anthropic API | curate_demos.py reads ANTHROPIC_API_KEY from env at build time; key never logged or written to demo.json (api_key leak check in script) |
| build-time -> frontend bundle | demo.json are committed static artifacts loaded in-memory at runtime; zero network |

No new runtime trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | ANTHROPIC_API_KEY in curation run | mitigate | curate_demos.py excludes api_key from session construction + leak check; operator never commits the key; demo.json verified key-free |
| T-04-06 | Tampering | committed demo.json artifacts | accept | Reproducible via curate_demos.py from committed input.json; round-trip + no-network tests gate integrity |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs (RESEARCH.md audit) |
</threat_model>

<verification>
- Provider/model check on all 3 demo.json prints anthropic claude-3-5-sonnet-latest.
- pnpm --filter @folio-mapper/web test --run src/__tests__/demo-mode-*.test.* and src/exemplar/demos/ green.
</verification>

<success_criteria>
- corporate-ma, ip-tech, commercial-lit curated with anthropic (visible mix) and registered.
- All 3 pass round-trip, no-network, and richness tests; full demo suite green for 7 areas.
</success_criteria>

<output>
Create `.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-04-SUMMARY.md` when done. Note any area shipped below the 2-enrichment richness floor and why (D-02 coherence override).
</output>
