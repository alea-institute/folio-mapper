---
phase: 04-demo-payloads-for-existing-exemplar-areas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/demos/run_probe.py
  - scripts/demos/verify_probe_items.py
  - scripts/demos/solo-criminal-probe-items.json
  - scripts/demos/family-law-probe-items.json
  - scripts/demos/employment-labor-probe-items.json
  - scripts/demos/corporate-ma-probe-items.json
  - scripts/demos/ip-tech-probe-items.json
  - scripts/demos/commercial-lit-probe-items.json
  - scripts/demos/real-estate-probe-items.json
  - scripts/demos/banking-finance-probe-items.json
  - scripts/demos/immigration-probe-items.json
autonomous: true
requirements: [DEMO-01, DEMO-03]
must_haves:
  truths:
    - "A single generalized probe script accepts --area {slug} and probes that area's lean items against FOLIO"
    - "Nine probe-items files exist, each holding the 16 verbatim lean items plus per-item level (root/branch/leaf) for one area"
    - "Running the probe for one area writes a candidates file an operator can read to pick enrichments (D-01: high_score_relevant >= 4 in coherent branches)"
  artifacts:
    - path: "scripts/demos/run_probe.py"
      provides: "Generalized coverage probe (--area arg) reusing spike-001 RELEVANT_BRANCHES + search loop"
      contains: "argparse"
    - path: "scripts/demos/solo-criminal-probe-items.json"
      provides: "16 lean items + item_level for solo-criminal"
      contains: "item_level"
  key_links:
    - from: "scripts/demos/run_probe.py"
      to: "scripts/demos/{slug}-probe-items.json"
      via: "reads items file by --area slug"
      pattern: "probe-items"
---

<objective>
Generalize spike-001's run_coverage_probe.py into a reusable scripts/demos/run_probe.py that takes --area {slug}, and author the 9 per-area {slug}-probe-items.json files (16 verbatim lean items plus level tags each). This is the executor-automatable half of D-01: it produces the probe tooling and inputs. The actual probe RUN (which loads FOLIO over the network) happens inside the per-area plans (Plans 03-05) using the backend venv, and its output drives enrichment selection.

Purpose: D-01 mandates probing each area before drafting enrichments, reusing the spike pattern (LLM-free, zero token cost). One generalized script plus 9 input files replaces copy-pasting the spike script 9 times.

Output: scripts/demos/run_probe.py (argparse --area), a small verify_probe_items.py checker, and 9 {slug}-probe-items.json files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-CONTEXT.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-RESEARCH.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md
@.planning/spikes/001-demo-pi-curation/run_coverage_probe.py
@.planning/spikes/001-demo-pi-curation/items.json

<interfaces>
<!-- Verbatim lean texts per slug from packages/core/src/exemplar/data.ts (04-PATTERNS.md table). -->
<!-- Each area: 1 root (0 tabs) + 5 branches (1 tab) + 10 leaves (2 tabs) = 16 items. -->
<!-- items array order is depth-first: root, then each branch followed by its leaves, in data.ts line order. -->

RELEVANT_BRANCHES (copy verbatim from spike run_coverage_probe.py lines 31-40):
Objectives, Area of Law, Legal Entity, Actor / Player, Service, Industry and Market, Matter Narrative, Standards Compatibility

items.json shape (from spike): practice_area (str), source (str), items (16 strings), item_level (16 strings of root/branch/leaf).
The authoritative per-slug label lists (root, 5 branches, 10 leaves) are in 04-PATTERNS.md "Verbatim lean texts per slug" table. Copy label strings exactly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize run_coverage_probe.py into scripts/demos/run_probe.py with --area</name>
  <files>scripts/demos/run_probe.py</files>
  <read_first>
    - .planning/spikes/001-demo-pi-curation/run_coverage_probe.py (the analog: copy RELEVANT_BRANCHES, the FOLIO search loop, the _branch_of helper verbatim)
    - .planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md (run_probe.py pattern section: generalization changes)
    - .planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-RESEARCH.md (Coverage-Probe Generalization section)
  </read_first>
  <action>
    Copy spike-001 run_coverage_probe.py into scripts/demos/run_probe.py. Replace the hardcoded ITEMS_FILE and OUT_FILE constants with an argparse --area {slug} argument; derive ITEMS_FILE as SCRIPT_DIR joined with {slug}-probe-items.json and OUT_FILE as SCRIPT_DIR joined with {slug}-probe-candidates.json, where SCRIPT_DIR is Path(__file__).resolve().parent (= scripts/demos/). Keep RELEVANT_BRANCHES, the folio.search_by_label call (include_alt_labels True, limit from arg), the high_score_relevant counting (score at least 90 AND branch in RELEVANT_BRANCHES), the per-item result dict fields (index, item, level, total_candidates, relevant_candidates, high_score_relevant, top_relevant, branches_touched), and the _branch_of helper verbatim. Add an optional --limit arg defaulting to 20. Print one stderr summary line per item showing the item label and its high_score_relevant count so the operator can eyeball results. Do NOT modify the spike's original script. Add a module docstring noting FOLIO() loads OWL from GitHub/cache (about 5-15s first run) and requires the backend venv (folio-python); run via backend/.venv/bin/python scripts/demos/run_probe.py --area {slug}.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-mapper" && python3 -m py_compile scripts/demos/run_probe.py && grep -q argparse scripts/demos/run_probe.py && grep -q RELEVANT_BRANCHES scripts/demos/run_probe.py && grep -q probe-items scripts/demos/run_probe.py && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - scripts/demos/run_probe.py compiles (py_compile succeeds).
    - Contains argparse with an --area argument and a --limit argument.
    - RELEVANT_BRANCHES set is byte-identical to the spike script's set.
    - Derives both probe-items.json (input) and probe-candidates.json (output) paths from the slug.
    - git diff shows no change to .planning/spikes/001-demo-pi-curation/run_coverage_probe.py.
  </acceptance_criteria>
  <done>Generalized probe script exists, compiles, takes --area, reuses the spike's search logic and RELEVANT_BRANCHES.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 9 probe-items.json files plus a structural checker</name>
  <files>scripts/demos/verify_probe_items.py, scripts/demos/solo-criminal-probe-items.json, scripts/demos/family-law-probe-items.json, scripts/demos/employment-labor-probe-items.json, scripts/demos/corporate-ma-probe-items.json, scripts/demos/ip-tech-probe-items.json, scripts/demos/commercial-lit-probe-items.json, scripts/demos/real-estate-probe-items.json, scripts/demos/banking-finance-probe-items.json, scripts/demos/immigration-probe-items.json</files>
  <read_first>
    - .planning/spikes/001-demo-pi-curation/items.json (exact shape: practice_area, source, items, item_level)
    - .planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md ("Verbatim lean texts per slug" table: authoritative root/branch/leaf labels for all 9 areas)
    - packages/core/src/exemplar/data.ts (cross-check exact label strings and the leaf-to-branch grouping/order if any PATTERNS.md row is ambiguous)
  </read_first>
  <action>
    For each of the 9 slugs, create scripts/demos/{slug}-probe-items.json mirroring the spike items.json shape: practice_area = slug; source = "packages/core/src/exemplar/data.ts (id: {slug})"; items = the 16 lean labels in depth-first order (root, then each branch immediately followed by its leaves, exactly as ordered in data.ts); item_level = a parallel array of root/branch/leaf (exactly 1 root, 5 branch, 10 leaf). Use the exact label strings from the 04-PATTERNS.md per-slug table; cross-check packages/core/src/exemplar/data.ts for the precise leaf grouping under each branch and preserve data.ts line order. Do NOT include any enrichments (the probe measures lean coverage only, per RESEARCH.md). Copy all label strings verbatim (no paraphrasing, no case changes). Also create scripts/demos/verify_probe_items.py: a tiny argparse-free checker that loads every {slug}-probe-items.json for the 9 slugs and asserts each has 16 items, 16 levels, exactly 1 root + 5 branch + 10 leaf, and item_level[0] == "root"; it prints PASS or the first failure and exits non-zero on failure. This checker is the Nyquist automated gate for this task.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-mapper" && python3 scripts/demos/verify_probe_items.py</automated>
  </verify>
  <acceptance_criteria>
    - All 9 {slug}-probe-items.json files are valid JSON with practice_area, source, items, item_level keys.
    - Each file: items length 16, item_level length 16, counts = 1 root / 5 branch / 10 leaf, first level is root.
    - Labels match the 04-PATTERNS.md per-slug table verbatim (spot-check at least the root + 5 branch labels per area against data.ts).
    - scripts/demos/verify_probe_items.py exits 0 and prints PASS.
  </acceptance_criteria>
  <done>Nine probe-items files exist with correct verbatim labels and level structure; checker passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → FOLIO (GitHub/cache) | run_probe.py loads the FOLIO OWL over the network at build/plan time; read-only fetch of a public ontology |

This is a developer-only build-time probe. No runtime trust boundary. No untrusted input: the probe items are repo-authored verbatim labels.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | FOLIO OWL fetched by run_probe.py | accept | folio-python fetches a pinned public ontology into ~/.folio/cache; output is read-only candidate counts, never executed |
| T-04-04 | Information Disclosure | probe-candidates.json output | accept | Contains only public FOLIO labels/scores; no secrets, no API keys (probe is LLM-free) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase (RESEARCH.md Package Legitimacy Audit: zero new packages; folio-python already in venv) |
</threat_model>

<verification>
- python3 -m py_compile scripts/demos/run_probe.py succeeds.
- python3 scripts/demos/verify_probe_items.py prints PASS and exits 0.
- git diff confirms the spike's original probe script is untouched.
</verification>

<success_criteria>
- Generalized run_probe.py (--area, --limit) reuses the spike's RELEVANT_BRANCHES and search loop.
- 9 probe-items.json files exist with correct verbatim labels and 1/5/10 level structure.
- A structural checker passes, satisfying the Nyquist automated-verify requirement.
</success_criteria>

<output>
Create `.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-02-SUMMARY.md` when done.
</output>
