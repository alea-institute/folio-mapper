---
phase: 04-demo-payloads-for-existing-exemplar-areas
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - scripts/demos/personal-injury.input.json
  - scripts/demos/solo-criminal.input.json
  - scripts/demos/family-law.input.json
  - scripts/demos/employment-labor.input.json
  - apps/web/src/exemplar/demos/personal-injury.demo.json
  - apps/web/src/exemplar/demos/solo-criminal.demo.json
  - apps/web/src/exemplar/demos/family-law.demo.json
  - apps/web/src/exemplar/demos/employment-labor.demo.json
  - apps/web/src/__tests__/demo-mode-roundtrip.test.tsx
  - apps/web/src/__tests__/demo-mode-no-network.test.tsx
  - apps/web/src/__tests__/demo-mode-richness.test.ts
autonomous: false
requirements: [DEMO-01, DEMO-02, DEMO-03, DEMO-04, DEMO-05]
user_setup:
  - service: anthropic
    why: "D-04 mandates LLM curation with claude-3-5-sonnet-latest; the executor has no API key or live backend in CI, so the operator runs curate_demos.py"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys (operator's own key; ~$0.50-$2 per area)"
    dashboard_config:
      - task: "Start the backend: pnpm dev:api (FastAPI on port 58000) before running curate_demos.py"
        location: "local shell"
must_haves:
  truths:
    - "The shipped PI demo is re-curated with --provider anthropic so provider/model are non-null and it carries real judge annotations (D-04 revised)"
    - "solo-criminal, family-law, and employment-labor each have an input.json (lean text verbatim + 2-3 enrichments placed thematically) and an anthropic-curated demo.json"
    - "Each of the 4 demo.json files loads via the round-trip path identically to a drag-drop session and shows a visible auto-accept/pending mix (0 < completed < total_nodes)"
    - "Zero LLM/pipeline network calls occur when any of these demos loads at runtime"
  artifacts:
    - path: "apps/web/src/exemplar/demos/solo-criminal.demo.json"
      provides: "Curated solo-criminal demo session (SessionFile v1.3, provider anthropic)"
      contains: "anthropic"
    - path: "scripts/demos/solo-criminal.input.json"
      provides: "Curation input: lean text + enrichments for solo-criminal"
      contains: "enrichments"
  key_links:
    - from: "apps/web/src/__tests__/demo-mode-richness.test.ts"
      to: "the 4 demo.json files"
      via: "it.each rows asserting 0 < completed < total_nodes"
      pattern: "toBeLessThan"
---

<objective>
Batch 1 of the per-area curation loop: re-curate the existing Personal Injury demo with the LLM (D-04 revised), then produce demo payloads for solo-criminal, family-law, and employment-labor. For each: probe (D-01), author the input.json, run the operator LLM curation at a tuned threshold (D-03), verify enrichment richness (D-02), and wire the demo.json into the manifest + parametrized test tables.

Purpose: PI re-curation makes all demos consistent (real judge annotations, non-null provider/model) and flips PI from its current 19/19 all-accepted state so the richness test can include it. The 3 new areas are the first installment of the 9 deferred-area demos (DEMO-01).

Output: 4 input.json + 4 demo.json (PI overwritten) + test rows for all 4 in the three harnesses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-CONTEXT.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-RESEARCH.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md
@scripts/demos/personal-injury.input.json
@scripts/curate_demos.py

<interfaces>
<!-- Curation CLI (verified from scripts/curate_demos.py). Reads scripts/demos/{slug}.input.json -> writes apps/web/src/exemplar/demos/{slug}.demo.json -->
backend/.venv/bin/python scripts/curate_demos.py --area {slug} --provider anthropic --threshold 0.85 --backend http://127.0.0.1:58000

<!-- curate_demos.py reads ONLY input_doc["text"]; other fields are documentary. -->
<!-- input.json shape (from personal-injury.input.json): slug, label, text (lean verbatim + enrichments inserted thematically as \t\t leaves), enrichments[], source_lean_exemplar, spike_reference -->
<!-- demo.json required fields: version "1.3", provider "anthropic", model "claude-3-5-sonnet-latest", screen "mapping", selections (string-keyed), node_statuses, pipeline_version, folio_version, total_nodes, completed -->
<!-- Slugs this batch: personal-injury (re-curate), solo-criminal, family-law, employment-labor -->
<!-- Test harness rows already scaffolded in Plan 01 (it.each tables + comments marking insertion points). -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Operator runs probe + LLM curation (PI re-curate + batch 1)</name>
  <what-built>Curation tooling, probe tooling (Plans 01-02), and the input.json authoring are executor-ready. The LLM curation itself requires the operator's ANTHROPIC_API_KEY and a running backend, which the executor cannot provide.</what-built>
  <action>Operator step (cannot be executor-automated): with backend running (pnpm dev:api, port 58000) and ANTHROPIC_API_KEY exported, run run_probe.py --area {slug} then curate_demos.py --area {slug} --provider anthropic --threshold 0.85 for personal-injury (re-curate per D-04), solo-criminal, family-law, and employment-labor. Tune --threshold per area until the stderr Auto-mapped ratio is a visible mix (0.55-0.80). Authoring of each input.json happens in Task 1; PI re-uses its existing input.json. Do not commit the API key.</action>
  <decision>Operator must run the probe + curation for this batch's areas.</decision>
  <how-to-verify>
    1. Ensure backend venv is set up (cd backend && pip install -e ".[embedding,nlp]" if not already).
    2. Start backend: pnpm dev:api (FastAPI on port 58000).
    3. export ANTHROPIC_API_KEY=sk-ant-... (your own key).
    4. For each slug in {personal-injury, solo-criminal, family-law, employment-labor}: after its input.json exists (authored in the next task), run:
       backend/.venv/bin/python scripts/demos/run_probe.py --area {slug}   (skip PI; its probe exists in the spike)
       backend/.venv/bin/python scripts/curate_demos.py --area {slug} --provider anthropic --threshold 0.85
    5. Read the stderr "Auto-mapped: X (unmapped: Y)" line. Target X/(X+Y) in 0.55-0.80. If all-accepted, re-run at 0.90 or 0.92; if all-pending, re-run at 0.75 or 0.80.
  </how-to-verify>
  <resume-signal>Type "curated" once all 4 demo.json files exist with provider anthropic, or describe blockers.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Author input.json for PI (re-confirm), solo-criminal, family-law, employment-labor</name>
  <files>scripts/demos/personal-injury.input.json, scripts/demos/solo-criminal.input.json, scripts/demos/family-law.input.json, scripts/demos/employment-labor.input.json</files>
  <read_first>
    - scripts/demos/personal-injury.input.json (the exact analog shape; enrichments placed thematically under the relevant branch, NOT at root)
    - scripts/demos/{slug}-probe-candidates.json for solo-criminal, family-law, employment-labor (operator-produced probe output: pick enrichments with high_score_relevant >= 4 in coherent branches; avoid the "Negligence trap")
    - .planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md ("Verbatim lean texts per slug" table + the input.json pattern section)
  </read_first>
  <action>
    For solo-criminal, family-law, employment-labor: create scripts/demos/{slug}.input.json matching the PI analog. Set slug, label (carousel label from RESEARCH.md slug table), source_lean_exemplar = "packages/core/src/exemplar/data.ts#{slug}", spike_reference = ".planning/spikes/001-demo-pi-curation/README.md". Build text from the verbatim lean hierarchy (root + 5 branches + 10 leaves, tab-indented exactly as data.ts, joined with \n) and insert 2-3 enrichment leaves as \t\t-prefixed lines INSIDE the thematically appropriate branch (per D-01/D-02: each enrichment chosen because the probe showed high_score_relevant >= 4 in a coherent branch; coherence outranks richness). Record the chosen enrichment labels in the enrichments[] array. For personal-injury: the input.json already exists and is correct (Insurance Bad Faith / Loss of Consortium / Class Action) - leave its text/enrichments unchanged; it is re-used verbatim for the D-04 re-curation. If the probe shows an area cannot reach 2 coherent enrichments, ship fewer rather than injecting off-domain noise and note the shortfall in the SUMMARY (per D-02).
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-mapper" && for s in personal-injury solo-criminal family-law employment-labor; do python3 -m json.tool "scripts/demos/$s.input.json" > /dev/null || exit 1; done && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 4 input.json files are valid JSON with slug, label, text, enrichments, source_lean_exemplar, spike_reference.
    - For the 3 new areas: text begins with the verbatim root line and contains all 5 branch + 10 leaf labels from the PATTERNS.md table, plus 2-3 enrichment leaves placed under thematically relevant branches.
    - enrichments[] lists exactly the enrichment labels inserted into text.
    - PI input.json is byte-identical to the pre-existing version.
  </acceptance_criteria>
  <done>Four input.json authored/confirmed; enrichments chosen from probe output, placed thematically.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the 4 curated demo.json + register in manifest + extend test tables</name>
  <files>apps/web/src/exemplar/demos/personal-injury.demo.json, apps/web/src/exemplar/demos/solo-criminal.demo.json, apps/web/src/exemplar/demos/family-law.demo.json, apps/web/src/exemplar/demos/employment-labor.demo.json, apps/web/src/__tests__/demo-mode-roundtrip.test.tsx, apps/web/src/__tests__/demo-mode-no-network.test.tsx, apps/web/src/__tests__/demo-mode-richness.test.ts</files>
  <read_first>
    - apps/web/src/__tests__/demo-mode-roundtrip.test.tsx (it.each table + insertion-point comment from Plan 01)
    - apps/web/src/__tests__/demo-mode-no-network.test.tsx (it.each table from Plan 01)
    - apps/web/src/__tests__/demo-mode-richness.test.ts (it.each table + PI it.todo from Plan 01)
    - apps/web/src/exemplar/demos/index.ts (LAZY_LOADERS already wired in Plan 01 — no change needed here, confirm slugs present)
    - .planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md (test extension patterns + Provider/Model verification one-liner)
  </read_first>
  <action>
    Precondition: the operator has produced all 4 demo.json files (checkpoint above). First validate each produced demo.json: provider == "anthropic", model == "claude-3-5-sonnet-latest", version == "1.3", and 0 < completed < total_nodes. If PI still shows completed == total_nodes, the operator must re-tune the threshold and re-curate before proceeding (D-03/D-04). The 9 new-area LAZY_LOADERS entries were already wired in Plan 01, so no index.ts edit is needed; just confirm solo-criminal/family-law/employment-labor are present in LAZY_LOADERS and all in DEMO_AVAILABLE_SLUGS. Extend the three test harnesses: add a static `import demoXxx from '../exemplar/demos/{slug}.demo.json'` for each of the 4 slugs and add a table row in roundtrip (it.each), no-network (it.each), and richness (it.each) for each. In richness.test.ts, MOVE personal-injury from the it.todo into the active it.each (it now passes because it was re-curated to a visible mix). Run the full demo test set to confirm all rows green.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-mapper" && pnpm --filter @folio-mapper/web test --run src/__tests__/demo-mode-roundtrip.test.tsx src/__tests__/demo-mode-no-network.test.tsx src/__tests__/demo-mode-richness.test.ts src/exemplar/demos/index.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 4 demo.json: provider "anthropic", model "claude-3-5-sonnet-latest", version "1.3", 0 < completed < total_nodes.
    - roundtrip, no-network, and richness test files each contain rows for all 4 slugs; PI is in the active richness it.each (not it.todo).
    - Full demo test set runs green (no failures, no skipped PI in richness).
    - DEMO_AVAILABLE_SLUGS still lists all 10 slugs; LAZY_LOADERS includes the 3 new slugs.
  </acceptance_criteria>
  <done>4 demo payloads validated, registered (via Plan 01 loaders), and green across round-trip, no-network, and richness tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> Anthropic API | curate_demos.py reads ANTHROPIC_API_KEY from env and calls the LLM at build time; key is never logged or written to the demo.json (curate_demos.py has an api_key leak check) |
| build-time -> frontend bundle | demo.json are committed static artifacts; loaded in-memory at runtime with zero network |

No new runtime trust boundary. The curation is a developer/operator build-time step.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | ANTHROPIC_API_KEY in curation run | mitigate | curate_demos.py excludes api_key from session construction (_build_session_file) and the leak check; operator never commits the key; demo.json verified to contain no key field |
| T-04-06 | Tampering | committed demo.json artifacts | accept | Reproducible via curate_demos.py from committed input.json; round-trip + no-network tests gate schema integrity |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs (RESEARCH.md audit: httpx + folio-python already in venv) |
</threat_model>

<verification>
- Provider/model check on all 4 demo.json prints anthropic claude-3-5-sonnet-latest.
- pnpm --filter @folio-mapper/web test --run src/__tests__/demo-mode-*.test.* and src/exemplar/demos/ green.
- PI demo no longer 19/19 (completed < total_nodes).
</verification>

<success_criteria>
- PI re-curated with anthropic (non-null provider/model, visible mix); solo-criminal, family-law, employment-labor curated and registered.
- All 4 pass round-trip, no-network, and richness tests; PI active in richness it.each.
</success_criteria>

<output>
Create `.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-03-SUMMARY.md` when done. Note any area that shipped below the 2-enrichment richness floor and why (D-02 coherence override).
</output>
