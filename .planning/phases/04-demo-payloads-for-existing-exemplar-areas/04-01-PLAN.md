---
phase: 04-demo-payloads-for-existing-exemplar-areas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/exemplar/demos/index.ts
  - apps/web/src/App.tsx
  - apps/web/src/exemplar/demos/index.test.ts
  - apps/web/src/__tests__/demo-mode-roundtrip.test.tsx
  - apps/web/src/__tests__/demo-mode-no-network.test.tsx
  - apps/web/src/__tests__/demo-mode-richness.test.ts
autonomous: true
requirements: [DEMO-01, DEMO-02, DEMO-04, DEMO-05]
must_haves:
  truths:
    - "getDemoPayload returns a Promise and resolves the PI payload eagerly"
    - "getDemoPayload returns null (not a hanging Promise) for slugs whose demo.json does not yet exist"
    - "App.tsx awaits getDemoPayload at the demo-mode call site"
    - "Test harnesses (roundtrip, no-network, richness, index) are parametrized over a slug+payload table that PI passes today and new areas plug into without rewrites"
  artifacts:
    - path: "apps/web/src/exemplar/demos/index.ts"
      provides: "Async getDemoPayload + LAZY_LOADERS map (10 slugs) + hardcoded DEMO_AVAILABLE_SLUGS"
      contains: "async function getDemoPayload"
    - path: "apps/web/src/__tests__/demo-mode-richness.test.ts"
      provides: "D-03 visible-mix assertion harness (0 < completed < total_nodes)"
      contains: "toBeLessThan"
  key_links:
    - from: "apps/web/src/App.tsx"
      to: "getDemoPayload"
      via: "await in handleExemplarSelect"
      pattern: "await getDemoPayload"
    - from: "apps/web/src/exemplar/demos/index.ts"
      to: "./{slug}.demo.json"
      via: "dynamic import() in LAZY_LOADERS"
      pattern: "import\\('\\./.*\\.demo\\.json'\\)"
---

<objective>
Migrate the demo manifest from eager synchronous imports to lazy `import()`-based async loading, update the single App.tsx call site to `await`, and parametrize the four test harnesses so that adding each new area later is a one-line table addition (Wave 0 test scaffolding per VALIDATION.md).

Purpose: The PI demo JSON is ~3.4 MB; adding 9 more eager imports would bloat the initial bundle by ~30-45 MB. Lazy loading keeps startup lean (Pitfall 3). The test parametrization is the Nyquist scaffold every per-area plan depends on.

Output: Async `getDemoPayload`, wired LAZY_LOADERS for all 10 slugs, hardcoded `DEMO_AVAILABLE_SLUGS`, awaited call site, and parametrized roundtrip / no-network / richness / index test harnesses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-CONTEXT.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-RESEARCH.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md

<interfaces>
<!-- Current contracts the executor must preserve. Extracted from codebase. -->

From apps/web/src/exemplar/demos/index.ts (current, lines 10-29):
```typescript
export interface DemoPayload {
  version: string;
  pipeline_version?: string;
  folio_version?: string;
  [key: string]: unknown;
}
export const DEMO_PAYLOADS: Record<string, DemoPayload> = { 'personal-injury': personalInjuryDemo as DemoPayload };
export function getDemoPayload(slug: string): DemoPayload | null { return DEMO_PAYLOADS[slug] ?? null; }
export const DEMO_AVAILABLE_SLUGS: ReadonlySet<string> = new Set(Object.keys(DEMO_PAYLOADS));
```
Lines 31-89 (RUNTIME_PIPELINE_VERSION, VersionVector, detectStalePreset, fetchRuntimeFolioVersion) are UNCHANGED — preserve verbatim.

From apps/web/src/App.tsx (line 549-554), handleExemplarSelect is already `async`:
```typescript
const handleExemplarSelect = async (id: string) => {
  ...
  if (exemplarMode === 'demo') {
    const payload = getDemoPayload(id);   // line 554 — needs `await`
```

The 10 canonical slugs (data.ts `id` values): personal-injury, solo-criminal, family-law, employment-labor, corporate-ma, ip-tech, commercial-lit, real-estate, banking-finance, immigration.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate index.ts to async lazy loading + await the App.tsx call site</name>
  <files>apps/web/src/exemplar/demos/index.ts, apps/web/src/App.tsx</files>
  <read_first>
    - apps/web/src/exemplar/demos/index.ts (the file being modified — current synchronous version)
    - apps/web/src/App.tsx lines 549-580 (the only getDemoPayload call site)
    - .planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md (the "index.ts (modify)" and "App.tsx (line 554, modify)" pattern sections — full target shapes)
  </read_first>
  <action>
    In index.ts: keep the eager `import personalInjuryDemo from './personal-injury.demo.json'` and the `DEMO_PAYLOADS` record holding only `'personal-injury'`. Add a `LAZY_LOADERS: Record<string, () => Promise<{ default: DemoPayload }>>` map with one entry per non-PI slug, each `() => import('./{slug}.demo.json')`, for all 9 new slugs (solo-criminal, family-law, employment-labor, corporate-ma, ip-tech, commercial-lit, real-estate, banking-finance, immigration). Add an in-memory `_demoCache: Record<string, DemoPayload>`. Convert `getDemoPayload` to `async function getDemoPayload(slug: string): Promise<DemoPayload | null>` that returns `DEMO_PAYLOADS[slug]` if present, else cached value, else awaits the lazy loader's `.default` and caches it, else returns null when no loader exists. Hardcode `DEMO_AVAILABLE_SLUGS` to a `new Set([...])` listing all 10 slugs literally (cannot derive from async loaders). Preserve lines 31-89 (RUNTIME_PIPELINE_VERSION, VersionVector, detectStalePreset, fetchRuntimeFolioVersion) verbatim. The 9 lazy `import()` targets do not exist yet — that is intentional and safe; Vite resolves them lazily and unproduced files simply never get requested (gated by DEMO_AVAILABLE_SLUGS membership + per-card availability). In App.tsx line 554: change `const payload = getDemoPayload(id)` to `const payload = await getDemoPayload(id)` (handleExemplarSelect is already async — confirmed line 549). Do NOT change any other line in App.tsx. This is the only call site (per PATTERNS.md — no other callers exist).
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-mapper" && grep -q "async function getDemoPayload" apps/web/src/exemplar/demos/index.ts && grep -q "await getDemoPayload" apps/web/src/App.tsx && pnpm --filter @folio-mapper/web exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "async function getDemoPayload" apps/web/src/exemplar/demos/index.ts` matches.
    - `grep "await getDemoPayload" apps/web/src/App.tsx` matches exactly one line.
    - `LAZY_LOADERS` contains exactly 9 entries (all non-PI slugs); `DEMO_AVAILABLE_SLUGS` set literal contains exactly 10 slugs.
    - `tsc --noEmit` for @folio-mapper/web reports no new type errors.
    - Lines 31-89 (detectStalePreset etc.) are byte-identical to the original.
  </acceptance_criteria>
  <done>getDemoPayload is async with lazy loaders for 9 new slugs, DEMO_AVAILABLE_SLUGS hardcoded to 10, App.tsx awaits it, typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parametrize roundtrip + no-network test harnesses and add the richness test (Wave 0 scaffolds)</name>
  <files>apps/web/src/__tests__/demo-mode-roundtrip.test.tsx, apps/web/src/__tests__/demo-mode-no-network.test.tsx, apps/web/src/__tests__/demo-mode-richness.test.ts, apps/web/src/exemplar/demos/index.test.ts</files>
  <behavior>
    - roundtrip: an `it.each` table currently holding only `['personal-injury', demoPI]` runs the identical PI round-trip body and passes; new rows are added later by per-area plans.
    - no-network: an `it.each` table holding only PI runs the zero-network assertion and passes; new rows added later.
    - richness: NEW test asserting `0 < completed < total_nodes`; PI is listed as `it.todo` / skipped because the SHIPPED PI demo is 19/19 (all-accepted) and will only pass after re-curation in Plan 03.
    - index.test.ts: a new test block asserts `DEMO_AVAILABLE_SLUGS` contains all 10 canonical slugs and `getDemoPayload('personal-injury')` resolves to a non-null payload with `version === '1.3'`.
  </behavior>
  <read_first>
    - apps/web/src/__tests__/demo-mode-roundtrip.test.tsx (current PI-only version — snapshot() + resetStores() helpers, the full test body to reuse)
    - apps/web/src/__tests__/demo-mode-no-network.test.tsx (current PI-only version — fetchSpy setup, offending-regex)
    - apps/web/src/exemplar/demos/index.test.ts (current detectStalePreset tests — append, don't replace)
    - .planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md (the roundtrip / no-network / richness pattern sections — exact it.each shapes)
  </read_first>
  <action>
    roundtrip.test.tsx: replace the single PI `it(...)` with `it.each([['personal-injury', demoPI]])('%s demo payload produces identical store state via both load paths', async (slug, payload) => { ... })` using the EXISTING PI test body verbatim (snapshot()/resetStores()/loadSessionFromObject/handleLoadSessionFile path), substituting `${slug}-session.json` for the filename. Keep the static `import demoPI from '../exemplar/demos/personal-injury.demo.json'`. Add a comment block listing where new `import demoXxx ...` lines and table rows get appended by later plans.
    no-network.test.tsx: add an `it.each([['personal-injury', demoPI]])('%s loadSessionFromObject performs zero LLM/pipeline/parse network calls', (slug, payload) => { ... })` reusing the existing offending-regex `/\/api\/(pipeline|llm|parse|embedding|mapping|export|github|synthetic|pricing)\b/` and `expect(offending).toEqual([])`. Keep existing PI it() blocks intact (do not delete the hydration test).
    NEW demo-mode-richness.test.ts: import { describe, it, expect } from 'vitest'; import demoPI; add `getRichness(payload)` helper reading top-level `total_nodes` and `completed`; `describe('demo mode richness (D-03)')` with `it.each([])` body asserting `completed > 0 && completed < total_nodes`. Because the SHIPPED PI demo is 19/19 (verified: completed===total_nodes===19), DO NOT put PI in the active it.each — use `it.todo('personal-injury — passes after Plan 03 re-curation')` so the suite is green now and PI joins the table in Plan 03. Add a comment listing where new rows get appended.
    index.test.ts: append (do not modify existing detectStalePreset tests) a `describe('demo manifest registration')` block: assert `DEMO_AVAILABLE_SLUGS` (imported from './index') is a Set whose size is 10 and contains 'personal-injury'; assert `await getDemoPayload('personal-injury')` is non-null with `.version === '1.3'`; assert `await getDemoPayload('does-not-exist')` resolves to null. Import getDemoPayload and DEMO_AVAILABLE_SLUGS from './index'.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-mapper" && pnpm --filter @folio-mapper/web test --run src/__tests__/demo-mode-roundtrip.test.tsx src/__tests__/demo-mode-no-network.test.tsx src/__tests__/demo-mode-richness.test.ts src/exemplar/demos/index.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All four test files run green (PI roundtrip + PI no-network pass; richness has PI as todo, zero failures; index registration block passes).
    - roundtrip and no-network use `it.each` with a single PI row each (verified by `grep "it.each" apps/web/src/__tests__/demo-mode-roundtrip.test.tsx`).
    - demo-mode-richness.test.ts exists and contains `toBeLessThan` and `it.todo`.
    - index.test.ts still contains all original detectStalePreset assertions plus the new registration block (`grep -c "detectStalePreset" apps/web/src/exemplar/demos/index.test.ts` unchanged from before).
  </acceptance_criteria>
  <done>Four harnesses parametrized/created and green; new areas plug in by adding one import + one table row each.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → frontend bundle | Lazy `import()` chunks are static JSON authored at build time; no runtime untrusted input crosses here |
| frontend → backend | Demo load path is purely in-memory (`loadSessionFromObject`); the only network call is a best-effort GET `/api/embedding/status` for the version probe — read-only, no payload |

This phase introduces no new runtime trust boundary. The curation script (Plans 03-05) is a developer-only build-time tool; it is not part of this foundation plan's surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | lazy `import()` of demo JSON | accept | Chunks are committed static artifacts built from the repo; no user-supplied path; slug is gated by hardcoded DEMO_AVAILABLE_SLUGS |
| T-04-02 | Denial of Service | unbounded `_demoCache` growth | accept | Cache bounded to ≤10 known slugs (LAZY_LOADERS keys); no user-controlled keys |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: zero new packages); nothing to gate |
</threat_model>

<verification>
- `pnpm --filter @folio-mapper/web exec tsc --noEmit` clean.
- `pnpm --filter @folio-mapper/web test --run src/__tests__/ src/exemplar/demos/` green.
- `grep "await getDemoPayload" apps/web/src/App.tsx` returns exactly one match.
</verification>

<success_criteria>
- getDemoPayload is async, lazy-loads 9 new slugs, eager-loads PI; DEMO_AVAILABLE_SLUGS hardcoded to 10.
- App.tsx awaits the call; typecheck and full frontend test suite green.
- Roundtrip, no-network, richness, and index harnesses parametrized so per-area plans add one row each.
</success_criteria>

<output>
Create `.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-01-SUMMARY.md` when done.
</output>
