---
phase: 04-demo-payloads-for-existing-exemplar-areas
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - scripts/demos/real-estate.input.json
  - scripts/demos/banking-finance.input.json
  - scripts/demos/immigration.input.json
  - apps/web/src/exemplar/demos/real-estate.demo.json
  - apps/web/src/exemplar/demos/banking-finance.demo.json
  - apps/web/src/exemplar/demos/immigration.demo.json
  - apps/web/src/__tests__/demo-mode-roundtrip.test.tsx
  - apps/web/src/__tests__/demo-mode-no-network.test.tsx
  - apps/web/src/__tests__/demo-mode-richness.test.ts
autonomous: false
requirements: [DEMO-01, DEMO-02, DEMO-03, DEMO-04, DEMO-05]
user_setup:
  - service: anthropic
    why: "D-04 LLM curation with claude-3-5-sonnet-latest; executor has no API key/live backend in CI"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "Anthropic Console -> API Keys (operator's own key)"
    dashboard_config:
      - task: "Start backend: pnpm dev:api (port 58000) before curating"
        location: "local shell"
must_haves:
  truths:
    - "real-estate, banking-finance, and immigration each have an input.json (lean verbatim + thematically placed enrichments) and an anthropic-curated demo.json"
    - "All 10 demos (PI + 9 areas) round-trip identically to drag-drop sessions, show a visible mix, and make zero LLM/pipeline network calls at runtime"
    - "DEMO_AVAILABLE_SLUGS, LAZY_LOADERS, and all three test tables cover all 10 slugs; the full frontend suite is green (phase gate)"
  artifacts:
    - path: "apps/web/src/exemplar/demos/immigration.demo.json"
      provides: "Curated immigration demo session (SessionFile v1.3, provider anthropic)"
      contains: "anthropic"
    - path: "scripts/demos/banking-finance.input.json"
      provides: "Curation input for banking-finance"
      contains: "enrichments"
  key_links:
    - from: "apps/web/src/__tests__/demo-mode-richness.test.ts"
      to: "all 10 demo.json files"
      via: "it.each rows asserting 0 < completed < total_nodes"
      pattern: "toBeLessThan"
---

<objective>
Batch 3 (final) of the per-area curation loop: produce demo payloads for real-estate, banking-finance, and immigration, completing all 9 deferred areas. Same loop as Plans 03/04. This plan closes the phase: after registering the last 3 areas, all 10 demos are covered and the full frontend test suite is the phase gate.

Purpose: Final installment of the 9 deferred-area demos; completes DEMO-01..05 across the entire existing exemplar set.

Output: 3 input.json + 3 demo.json + final test rows; full-suite green gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-CONTEXT.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-RESEARCH.md
@.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md
@scripts/demos/personal-injury.input.json
@scripts/curate_demos.py

<interfaces>
<!-- Curation CLI: backend/.venv/bin/python scripts/curate_demos.py --area {slug} --provider anthropic --threshold 0.85 --backend http://127.0.0.1:58000 -->
<!-- input.json shape: slug, label, text (lean verbatim + enrichments inserted thematically as tab-tab leaves), enrichments[], source_lean_exemplar, spike_reference. curate reads ONLY text. -->
<!-- demo.json required: version "1.3", provider "anthropic", model "claude-3-5-sonnet-latest", screen "mapping", selections (string-keyed), node_statuses, pipeline_version, folio_version, total_nodes, completed -->
<!-- Slugs this batch: real-estate, banking-finance, immigration -->
<!-- Test harness tables already hold PI + batch-1 + batch-2 rows (7 areas); append the final 3 rows. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Operator runs probe + LLM curation (batch 3, final)</name>
  <what-built>Probe + curation tooling ready (Plans 01-02). The LLM curation requires the operator's ANTHROPIC_API_KEY and a running backend; the executor cannot run it.</what-built>
  <action>Operator step (cannot be executor-automated): with backend running (pnpm dev:api, port 58000) and ANTHROPIC_API_KEY exported, run run_probe.py --area {slug} then curate_demos.py --area {slug} --provider anthropic --threshold 0.85 for real-estate, banking-finance, and immigration (input.json authored in Task 1 first). Tune --threshold per area for a visible mix (0.55-0.80). Do not commit the API key.</action>
  <decision>Operator runs the probe + curation for real-estate, banking-finance, immigration.</decision>
  <how-to-verify>
    1. Backend running: pnpm dev:api (port 58000). 2. export ANTHROPIC_API_KEY=sk-ant-...
    3. For each slug in {real-estate, banking-finance, immigration}: after its input.json exists, run run_probe.py --area {slug} then curate_demos.py --area {slug} --provider anthropic --threshold 0.85.
    4. Read stderr "Auto-mapped: X (unmapped: Y)". Target X over (X+Y) in 0.55-0.80. All-accepted means raise to 0.90/0.92; all-pending means lower to 0.75/0.80.
  </how-to-verify>
  <resume-signal>Type "curated" once all 3 demo.json exist with provider anthropic, or describe blockers.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Author input.json for real-estate, banking-finance, immigration</name>
  <files>scripts/demos/real-estate.input.json, scripts/demos/banking-finance.input.json, scripts/demos/immigration.input.json</files>
  <read_first>
    - scripts/demos/personal-injury.input.json (analog shape; enrichments placed under thematically relevant branch)
    - scripts/demos/{slug}-probe-candidates.json for the 3 slugs (operator probe output: pick enrichments with high_score_relevant >= 4 in coherent branches; avoid trap terms)
    - .planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md ("Verbatim lean texts per slug" table + input.json pattern)
  </read_first>
  <action>
    For each of real-estate, banking-finance, immigration: create scripts/demos/{slug}.input.json matching the PI analog. Set slug, label (carousel label from RESEARCH.md slug table: Real Estate, Banking & Finance, Immigration), source_lean_exemplar = packages/core/src/exemplar/data.ts#{slug}, spike_reference = .planning/spikes/001-demo-pi-curation/README.md. Build text from the verbatim lean hierarchy (root + 5 branches + 10 leaves, tab-indented as in data.ts, joined with newlines), inserting 2-3 enrichment leaves as tab-tab lines under the thematically appropriate branch. Choose enrichments from probe output where high_score_relevant >= 4 in a coherent branch. Per Pitfall 6 / D-02: if banking-finance (a thinner transactional area) yields only 1-2 coherent enrichments, ship that many rather than forcing noise, and note the shortfall in the SUMMARY. Record chosen labels in enrichments[].
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-mapper" && for s in real-estate banking-finance immigration; do python3 -m json.tool "scripts/demos/$s.input.json" > /dev/null || exit 1; done && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 3 input.json files are valid JSON with slug, label, text, enrichments, source_lean_exemplar, spike_reference.
    - text begins with the verbatim root line and contains all 5 branch + 10 leaf labels from the PATTERNS.md table for each slug, plus enrichment leaves under thematically relevant branches.
    - enrichments[] lists exactly the enrichment labels inserted into text.
  </acceptance_criteria>
  <done>Three input.json authored; enrichments chosen from probe output, placed thematically.</done>
</task>

<task type="auto">
  <name>Task 2: Validate the final 3 demo.json, complete test tables, and run the phase-gate full suite</name>
  <files>apps/web/src/exemplar/demos/real-estate.demo.json, apps/web/src/exemplar/demos/banking-finance.demo.json, apps/web/src/exemplar/demos/immigration.demo.json, apps/web/src/__tests__/demo-mode-roundtrip.test.tsx, apps/web/src/__tests__/demo-mode-no-network.test.tsx, apps/web/src/__tests__/demo-mode-richness.test.ts</files>
  <read_first>
    - apps/web/src/__tests__/demo-mode-roundtrip.test.tsx (it.each table with 7 rows; append final 3)
    - apps/web/src/__tests__/demo-mode-no-network.test.tsx (it.each table; append)
    - apps/web/src/__tests__/demo-mode-richness.test.ts (it.each table; append)
    - apps/web/src/exemplar/demos/index.ts (confirm all 10 slugs in LAZY_LOADERS keys + personal-injury eager + DEMO_AVAILABLE_SLUGS)
    - .planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-PATTERNS.md (test extension patterns + Provider/Model verification one-liner)
  </read_first>
  <action>
    Precondition: operator produced all 3 demo.json (checkpoint above). Validate each: provider equals anthropic, model equals claude-3-5-sonnet-latest, version equals 1.3, and 0 < completed < total_nodes (re-tune + re-curate if all-accepted or all-pending). LAZY_LOADERS were wired in Plan 01 so no index.ts edit needed; confirm all 10 slugs present (1 eager PI + 9 lazy). Extend the three test harnesses with a static import + table row for each of the 3 final slugs. The three tables must now each list all 10 slugs. Then run the FULL frontend + packages test suite (pnpm test) as the phase gate, not just the demo subset, to confirm no regression elsewhere.
  </action>
  <verify>
    <automated>cd "/home/damienriehl/Coding Projects/folio-mapper" && pnpm test 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 3 final demo.json: provider anthropic, model claude-3-5-sonnet-latest, version 1.3, 0 < completed < total_nodes.
    - roundtrip, no-network, and richness it.each tables each contain exactly 10 rows (all slugs).
    - DEMO_AVAILABLE_SLUGS lists all 10 slugs; LAZY_LOADERS has 9 entries (PI eager).
    - pnpm test (full suite) is green.
  </acceptance_criteria>
  <done>All 9 deferred areas + re-curated PI shipped, registered, and green; full suite passes as the phase gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> Anthropic API | curate_demos.py reads ANTHROPIC_API_KEY from env at build time; key never logged or written to demo.json (api_key leak check in script) |
| build-time -> frontend bundle | demo.json are committed static artifacts loaded in-memory at runtime; zero network |

No new runtime trust boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Information Disclosure | ANTHROPIC_API_KEY in curation run | mitigate | curate_demos.py excludes api_key from session construction + leak check; operator never commits the key; demo.json verified key-free |
| T-04-06 | Tampering | committed demo.json artifacts | accept | Reproducible via curate_demos.py from committed input.json; round-trip + no-network tests gate integrity |
| T-04-07 | Denial of Service | 10 lazy demo chunks loaded on demand | accept | Each chunk loaded only when its card is clicked; cache bounded to 10 known slugs; no startup bundle bloat (the bundle-size mitigation from Plan 01) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs (RESEARCH.md audit) |
</threat_model>

<verification>
- Provider/model check on the final 3 demo.json prints anthropic claude-3-5-sonnet-latest.
- All three test tables have 10 rows.
- pnpm test (full suite) green.
</verification>

<success_criteria>
- real-estate, banking-finance, immigration curated with anthropic (visible mix) and registered.
- All 10 demos covered across round-trip, no-network, richness; full frontend + packages suite green.
- DEMO-01..05 satisfied for the entire existing exemplar set.
</success_criteria>

<output>
Create `.planning/phases/04-demo-payloads-for-existing-exemplar-areas/04-05-SUMMARY.md` when done. Note any area shipped below the 2-enrichment richness floor and why (D-02 coherence override), and confirm zero new packages installed.
</output>
