---
phase: 06-demo-payloads-for-regulatory-exemplars
plan: 01
type: execute
wave: 1
depends_on: [05-01]
autonomous: false
requirements: [REGDEMO-01, REGDEMO-02, REGDEMO-03]
files_modified:
  - scripts/demos/{slug}-probe-items.json (×4)
  - scripts/demos/{slug}.input.json (×4)
  - apps/web/src/exemplar/demos/{slug}.demo.json (×4)
  - apps/web/src/exemplar/demos/index.ts
  - apps/web/src/__tests__/demo-mode-roundtrip.test.tsx
  - apps/web/src/__tests__/demo-mode-no-network.test.tsx
  - apps/web/src/__tests__/demo-mode-richness.test.ts
  - apps/web/src/__tests__/demo-mode-excluded-branches.test.ts
  - apps/web/src/__tests__/demo-mode-smoke.test.ts
  - apps/web/src/exemplar/demos/index.test.ts
user_setup:
  service: "Backend (isolated port) + LLM judge key"
  why: "curate_demos.py drives the live backend LLM judge (final pass: Gemini, matching the existing 10-demo standard)"
  env_vars: "GOOGLE_API_KEY (final pass); ANTHROPIC_BASE_URL=http://127.0.0.1:8788 + dummy key (free iteration via Claude Max proxy)"
must_haves:
  truths:
    - A demo payload exists for each of the 4 new regulatory exemplars.
    - Each demo loads via the Stage-7A session-load path with zero runtime network calls.
    - Each payload has provider=google, model=gemini-3-flash-preview, version=1.3, a visible auto-accept/pending mix, and real Stage-3 judge annotations.
    - All 4 slugs are registered in DEMO_AVAILABLE_SLUGS and every demo test harness.
  artifacts:
    - path: apps/web/src/exemplar/demos/environmental-compliance.demo.json
      provides: curated cached pipeline output, 18 nodes, visible mix, judge annotations
    - path: apps/web/src/exemplar/demos/energy-utilities.demo.json
      provides: curated cached pipeline output
    - path: apps/web/src/exemplar/demos/securities-regulation.demo.json
      provides: curated cached pipeline output
    - path: apps/web/src/exemplar/demos/data-privacy.demo.json
      provides: curated cached pipeline output
  key_links:
    - from: getDemoPayload
      to: ./{slug}.demo.json
      via: import.meta.glob lazy loader
      pattern: glob auto-registers each new demo.json
    - from: App.tsx handleExemplarSelect
      to: getDemoPayload
      via: await in demo mode
      pattern: unchanged from Phase 4
---

<objective>
Curate and ship a cached demo payload for each of the 4 regulatory exemplars, using
the locked Phase-4 curation pattern, so every carousel card is demo-able with zero
runtime LLM cost. Verify in-app.
</objective>

<context>
Read: 06-CONTEXT.md, 04-CONTEXT.md, scripts/curate_demos.py, personal-injury.demo.json.
I/O generated by gen_regulatory_probe_items.py + gen_regulatory_inputs.py.
</context>

<tasks>
<task type="checkpoint:human-action" gate="blocking">
  <name>Run the coverage probe + LLM curation</name>
  <what-built>4 probe-items + 4 input.json (generated); curation stack (proxy on
  8788, backend on 58001 with embedding index built).</what-built>
  <action>For each slug: run_probe.py to confirm fan-out, then
  curate_demos.py --area {slug} --provider google --backend http://127.0.0.1:58001
  --threshold 0.3 --accept-threshold 0.9.</action>
  <decision>Confirm each payload has a visible mix (0 < completed < total) and real
  judge annotations; re-tune threshold if all-accepted or all-pending.</decision>
  <how-to-verify>1. provider=google, model=gemini-3-flash-preview, version=1.3 in each demo.json. 2. completed
  strictly between 0 and total_nodes. 3. stage3 judge counts > 0.</how-to-verify>
  <resume-signal>4 demo.json committed under apps/web/src/exemplar/demos/.</resume-signal>
</task>

<task type="auto">
  <name>Register + update test harnesses</name>
  <files>index.ts + 6 test files</files>
  <action>Add 4 slugs to DEMO_AVAILABLE_SLUGS; add static import + it.each row to the
  4 payload tests; extend SLUGS/CANONICAL_SLUGS and bump size assertions to 14.</action>
  <verify><automated>pnpm --filter @folio-mapper/web test</automated></verify>
  <acceptance_criteria>Full web suite green with 14 demo areas covered.</acceptance_criteria>
</task>

<task type="auto">
  <name>In-app browser verification</name>
  <files>—</files>
  <action>Start dev servers; toggle Demo mode; select each new card; confirm cached
  pipeline output renders (candidates, judge annotations, auto-accept mix) with zero
  /api/pipeline calls.</action>
  <verify><automated>chrome-devtools screenshot per area; network panel shows no pipeline call</automated></verify>
  <acceptance_criteria>All 4 regulatory demos load and render correctly.</acceptance_criteria>
</task>
</tasks>

<success_criteria>
Maps to ROADMAP Phase 6 SC-1..SC-3: a curated payload per new exemplar (SC-1);
loads via Stage-7A and registered in the manifest (SC-2); zero runtime LLM calls (SC-3).
</success_criteria>

<output>Create 06-01-SUMMARY.md on completion.</output>
