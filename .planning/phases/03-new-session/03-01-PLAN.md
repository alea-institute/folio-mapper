---
phase: 03-new-session
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/store/tab-identity.ts
  - apps/web/src/store/session-registry.ts
  - apps/web/src/store/session-storage.ts
  - apps/web/src/store/tab-identity.test.ts
  - apps/web/src/store/session-registry.test.ts
autonomous: true
requirements: [D-04, D-05, D-06, D-09, D-13, D-14]
must_haves:
  truths:
    - "A ?new=1 URL generates a fresh tab id in sessionStorage and strips the query param"
    - "A refresh within a tab reuses the same sessionStorage tab id (D-08 path resolves here)"
    - "Legacy single-session localStorage data is copied to namespaced keys on first boot with no data loss"
    - "Writing a 6th session evicts the least-recently-modified session's data from localStorage"
    - "The session registry sorts by updatedAt so the most-recently-modified session is rank 0"
    - "Session data lives in localStorage and survives a full browser close (D-13)"
  artifacts:
    - path: "apps/web/src/store/tab-identity.ts"
      provides: "Synchronous module-level tab identity resolver + namespaced key exports"
      contains: "export function resolveTabIdentity"
    - path: "apps/web/src/store/session-registry.ts"
      provides: "Registry CRUD with LRU eviction"
      exports: ["readRegistry", "upsertRegistry", "deleteFromRegistry", "SessionRecord"]
    - path: "apps/web/src/store/session-storage.ts"
      provides: "Debounced storage adapter with onWrite callback for registry updates"
      contains: "onWrite"
    - path: "apps/web/src/store/tab-identity.test.ts"
      provides: "Boot-path tests for D-05, D-06, D-08 (authored in Task 1)"
    - path: "apps/web/src/store/session-registry.test.ts"
      provides: "Registry tests for D-07 sort, D-09 eviction, D-14 updatedAt (authored in Task 2)"
  key_links:
    - from: "apps/web/src/store/tab-identity.ts"
      to: "sessionStorage 'folio-tab-id'"
      via: "getItem/setItem at module load"
      pattern: "folio-tab-id"
    - from: "apps/web/src/store/session-registry.ts"
      to: "localStorage 'folio-mapper-session-registry'"
      via: "read/upsert/delete"
      pattern: "folio-mapper-session-registry"
---

<objective>
Build the per-tab session storage core: a synchronous tab-identity resolver, a session
registry with LRU eviction, and an extension to the debounced storage adapter that updates
the registry on write. This is the foundation the stores and boot resolver consume in later
waves. No React, no UI — pure modules and Web APIs.

Tests are authored tests-first, alongside the module they cover (VALIDATION.md Wave 0): the
tab-identity tests land in Task 1 with tab-identity.ts, and the session-registry tests land in
Task 2 with session-registry.ts. This keeps each task's own `<automated>` test pattern matching
a real file so vitest never exits non-zero with "No test files found".

Purpose: Replace the single shared localStorage session with a per-tab namespaced model
(D-04) that survives browser close (D-13), migrates legacy data (D-06), and is bounded by
an LRU cap (D-09) keyed on last-modified time (D-14).
Output: tab-identity.ts (+ tab-identity.test.ts), session-registry.ts (+ session-registry.test.ts),
and an extended session-storage.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-new-session/03-CONTEXT.md
@.planning/phases/03-new-session/03-RESEARCH.md
@.planning/phases/03-new-session/03-PATTERNS.md
@.planning/phases/03-new-session/03-VALIDATION.md

<interfaces>
<!-- Key contracts the executor must produce. Implement against these exactly. -->

Tab identity resolution (tab-identity.ts) — resolved synchronously at module load:
  resolveTabIdentity(): { tabId: string; isNewTab: boolean; hasIdentity: boolean }
  export const tabIdentity = resolveTabIdentity();
  export const MAPPING_KEY: string;   // `folio-mapper-session-${tabId}-mapping` or placeholder
  export const INPUT_KEY: string;     // `folio-mapper-session-${tabId}-input` or placeholder

Key scheme (Claude's discretion, locked by RESEARCH.md Pattern 4):
  - sessionStorage tab-identity pointer: 'folio-tab-id'
  - per-tab mapping data: `folio-mapper-session-${tabId}-mapping`
  - per-tab input data:   `folio-mapper-session-${tabId}-input`
  - global registry:      'folio-mapper-session-registry'
  - legacy keys (migrate from): 'folio-mapper-session-mapping', 'folio-mapper-session-input'
  - LLM store key (DO NOT TOUCH, stays global): 'folio-mapper-llm'

Session registry record (session-registry.ts):
  interface SessionRecord {
    tabId: string;
    updatedAt: string;   // ISO 8601, bumped only inside debounced setItem
    createdAt: string;   // ISO 8601, set once on first upsert
    totalNodes: number;
    completed: number;
    skipped: number;
    sourceFile: string | null;
  }
  readRegistry(): SessionRecord[]
  upsertRegistry(record: SessionRecord): void   // enforces LRU cap MAX_SESSIONS = 5
  deleteFromRegistry(tabId: string): void

Debounced storage extension (session-storage.ts):
  createDebouncedStorage(opts?: { onWrite?: (name: string) => void }): StateStorage
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tab-identity.ts synchronous resolver with legacy migration (+ tests, tests-first)</name>
  <files>apps/web/src/store/tab-identity.ts, apps/web/src/store/tab-identity.test.ts</files>
  <read_first>
    - .planning/phases/03-new-session/03-RESEARCH.md (Pattern 1, lines 168-238; Pitfall 2 lines 453-457)
    - .planning/phases/03-new-session/03-PATTERNS.md (tab-identity.ts section, lines 31-83; Vitest test structure lines 646-679)
    - apps/web/src/store/session-storage.ts (analog: QuotaExceeded try/catch, lines 29-39)
    - apps/web/src/store/input-store.test.ts (analog: store unit test structure)
    - apps/web/src/store/demo-store.test.ts (analog: localStorage key-diff assertions ~lines 88-102)
    - /home/damienriehl/Coding Projects/folio-enrich/frontend/index.html (READ-ONLY: lines ~3593-3595, the ?new=1 detection + history.replaceState pattern)
  </read_first>
  <behavior>
    Write apps/web/src/store/tab-identity.test.ts FIRST (RED), then implement tab-identity.ts to make it GREEN.
    In beforeEach clear localStorage + sessionStorage. resolveTabIdentity runs at module import, so
    re-run the resolver per test via vi.resetModules() + dynamic import().
    - D-05: set window.location.search to '?new=1' → a new 'folio-tab-id' appears in sessionStorage and isNewTab is true.
    - D-08: pre-seed 'folio-tab-id' → it is reused, isNewTab false, hasIdentity true.
    - D-06: pre-seed legacy 'folio-mapper-session-mapping' value → it is copied to the namespaced key,
      the legacy key is removed, no data is lost.
  </behavior>
  <action>
    Create apps/web/src/store/tab-identity.test.ts FIRST (the cases above) and confirm it fails
    (RED) because tab-identity.ts does not exist yet. Then create
    apps/web/src/store/tab-identity.ts as a module with NO framework imports (browser
    globals only: URLSearchParams, sessionStorage, localStorage, crypto, history). Implement
    resolveTabIdentity() returning { tabId, isNewTab, hasIdentity } following RESEARCH.md
    Pattern 1 exactly:
      (1) D-05: if URLSearchParams(window.location.search).has('new') → tabId =
          crypto.randomUUID(), sessionStorage.setItem('folio-tab-id', tabId),
          history.replaceState(null, '', window.location.pathname), return isNewTab:true,
          hasIdentity:true. The replaceState MUST pass window.location.pathname only — never a
          user-controlled or query-derived URL (open-redirect/injection guard).
      (2) D-08: else if sessionStorage.getItem('folio-tab-id') exists → return that id,
          isNewTab:false, hasIdentity:true.
      (3) D-06: else if either legacy key ('folio-mapper-session-mapping' or
          'folio-mapper-session-input') is present in localStorage → generate migratedId,
          store it in sessionStorage, call migrateToNamespacedKeys(migratedId), return
          isNewTab:false, hasIdentity:true.
      (4) D-07 fallback: else return { tabId: '', isNewTab:false, hasIdentity:false } (boot
          resolver in Plan 02 handles auto-resume).
    Implement migrateToNamespacedKeys(tabId) centrally HERE (Pitfall 2 — never in the stores):
    copy each legacy value to `folio-mapper-session-${tabId}-{mapping|input}`, then
    localStorage.removeItem the legacy key. Export `const tabIdentity = resolveTabIdentity()`
    (module-level side effect, runs once at import). Export MAPPING_KEY and INPUT_KEY derived
    from tabIdentity.tabId, falling back to a placeholder key when tabId is '' (so the
    no-identity path uses a safe placeholder until the boot resolver rewires it). Wrap
    localStorage writes in the QuotaExceeded try/catch from session-storage.ts lines 29-39.
    Because the test file is authored in this task, the `tab-identity` test pattern matches a real
    file and the verify command resolves (no "No test files found" non-zero exit).
  </action>
  <verify>
    <automated>pnpm --filter @folio-mapper/web test --run -- tab-identity</automated>
  </verify>
  <acceptance_criteria>
    - tab-identity.test.ts exists and asserts D-05, D-06, D-08 behaviors (authored in this task)
    - tab-identity.ts contains `export function resolveTabIdentity(` and `export const tabIdentity`
    - tab-identity.ts contains `export const MAPPING_KEY` and `export const INPUT_KEY`
    - history.replaceState call uses window.location.pathname only (grep: `replaceState` line references `window.location.pathname`, never `.search` or `params`)
    - No `import` of zustand/react in tab-identity.ts (grep -v '^//' tab-identity.ts | grep -c "from 'zustand'" returns 0)
    - `pnpm --filter @folio-mapper/web test --run -- tab-identity` passes (the pattern matches the test file created in this task — no "No test files found" exit)
  </acceptance_criteria>
  <done>tab-identity.test.ts and tab-identity.ts both exist; resolveTabIdentity returns the correct branch for ?new=1, existing-identity, legacy-data, and no-data cases; legacy keys are migrated to namespaced keys and removed; the tab-identity test command passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create session-registry.ts with LRU eviction (D-09/D-14) (+ tests, tests-first)</name>
  <files>apps/web/src/store/session-registry.ts, apps/web/src/store/session-registry.test.ts</files>
  <read_first>
    - .planning/phases/03-new-session/03-RESEARCH.md (Pattern 4, lines 330-396; Pitfall 4 lines 465-469)
    - .planning/phases/03-new-session/03-PATTERNS.md (session-registry.ts section, lines 87-141; Vitest test structure lines 646-679)
    - apps/web/src/store/session-storage.ts (analog: QuotaExceeded guard lines 29-39)
    - apps/web/src/store/demo-store.test.ts (analog test pattern, localStorage key-diff assertions ~lines 88-102)
    - apps/web/src/store/input-store.test.ts (analog: store unit test structure)
  </read_first>
  <behavior>
    Write apps/web/src/store/session-registry.test.ts FIRST (RED), then implement session-registry.ts to make it GREEN.
    In beforeEach clear localStorage.
    - D-14: upsert a record with a known updatedAt → readRegistry returns it sorted most-recent first.
    - D-09: upsert 6 distinct tabIds → registry length === 5 and the least-recently-modified tabId's
      two namespaced data keys are removed from localStorage.
    - D-07-support: after sort, the most-recently-modified record is rank 0.
    - delete: deleteFromRegistry removes the record and its two namespaced data keys.
  </behavior>
  <action>
    Create apps/web/src/store/session-registry.test.ts FIRST (the cases above) and confirm it fails
    (RED) because session-registry.ts does not exist yet. Then create
    apps/web/src/store/session-registry.ts as pure named functions over localStorage
    (no framework imports). Define and export the SessionRecord interface and constants
    REGISTRY_KEY = 'folio-mapper-session-registry' and MAX_SESSIONS = 5. Implement:
      - readRegistry(): SessionRecord[] — JSON.parse wrapped in try/catch returning [] on
        failure (per PATTERNS.md JSON.parse safety pattern).
      - upsertRegistry(record): D-14 — find existing record by tabId; replace it (or push if
        new) with the supplied record (caller stamps updatedAt at write time). Then sort
        descending by new Date(updatedAt).getTime(), splice off everything past MAX_SESSIONS,
        and for each evicted record localStorage.removeItem the
        `folio-mapper-session-${tabId}-{mapping|input}` keys (D-09). CRITICAL (Pitfall 4):
        the supplied record's updatedAt is "now", so the current tab is always rank 0 and
        never evicted. Persist the trimmed array with the QuotaExceeded try/catch.
      - deleteFromRegistry(tabId): filter the record out, remove its two namespaced data keys,
        write the filtered array back.
    Do NOT touch 'folio-mapper-llm'. Registry holds only UI metadata (no secrets) per the
    threat model. Because the test file is authored in this task, the `session-registry` test
    pattern matches a real file and the verify command resolves (no "No test files found" exit).
  </action>
  <verify>
    <automated>pnpm --filter @folio-mapper/web test --run -- session-registry</automated>
  </verify>
  <acceptance_criteria>
    - session-registry.test.ts exists and asserts D-09 eviction, D-14 sort, and delete behaviors (authored in this task)
    - session-registry.ts exports `readRegistry`, `upsertRegistry`, `deleteFromRegistry`, and `SessionRecord`
    - session-registry.ts contains `const MAX_SESSIONS = 5` and `'folio-mapper-session-registry'`
    - upsertRegistry sorts by updatedAt desc and splices to MAX_SESSIONS (grep: `MAX_SESSIONS` appears in a splice/slice call)
    - `pnpm --filter @folio-mapper/web test --run -- session-registry` passes (the pattern matches the test file created in this task — no "No test files found" exit)
  </acceptance_criteria>
  <done>session-registry.test.ts and session-registry.ts both exist; registry reads/writes safely; a 6th distinct session evicts the least-recently-modified one and removes its localStorage data keys; the current tab is never evicted; the session-registry test command passes.</done>
</task>

<task type="auto">
  <name>Task 3: Extend session-storage.ts with onWrite callback + confirm full Wave-0 coverage</name>
  <files>apps/web/src/store/session-storage.ts</files>
  <read_first>
    - apps/web/src/store/session-storage.ts (self — full file, 60 lines; extend createDebouncedStorage)
    - .planning/phases/03-new-session/03-RESEARCH.md (Pattern 4 integration note line 396; Pitfall 3 lines 459-463)
    - .planning/phases/03-new-session/03-PATTERNS.md (session-storage.ts modify section lines 144-169)
    - apps/web/src/store/tab-identity.test.ts (created in Task 1 — already covers D-05, D-06, D-08)
    - apps/web/src/store/session-registry.test.ts (created in Task 2 — already covers D-09, D-14, delete)
  </read_first>
  <action>
    Modify createDebouncedStorage in apps/web/src/store/session-storage.ts to accept an optional
    `opts?: { onWrite?: (name: string) => void }` parameter. Inside the setTimeout callback,
    AFTER localStorage.setItem succeeds, call opts?.onWrite?.(name). Keep the existing signature
    callable with no args so llm-store.ts usage is unchanged. This is the single hook through
    which the registry's updatedAt is bumped at debounced-write time (Pitfall 3 — never on
    in-memory store mutation). Leave flushDebouncedStorage as-is.

    The two Wave-0 test files (tab-identity.test.ts, session-registry.test.ts) were authored in
    Tasks 1 and 2 respectively. In this task, run the FULL web suite to confirm no regression and
    that all Wave-0 coverage is green together. If the onWrite extension is unit-testable inline,
    add a small assertion to an existing storage test (optional — not required for Wave 0). Do NOT
    re-create the tab-identity or session-registry test files; they already exist.
  </action>
  <verify>
    <automated>pnpm --filter @folio-mapper/web test --run</automated>
  </verify>
  <acceptance_criteria>
    - session-storage.ts contains `onWrite` and calls it inside the setTimeout after setItem
    - createDebouncedStorage() still callable with zero args (grep: existing llm-store import is unbroken — full `pnpm --filter @folio-mapper/web test --run` green)
    - tab-identity.test.ts (from Task 1) and session-registry.test.ts (from Task 2) both pass within the full run
    - `pnpm --filter @folio-mapper/web test --run` passes (no regression in existing store tests)
  </acceptance_criteria>
  <done>onWrite callback fires after debounced localStorage write; the full web test suite — including the Wave-0 tab-identity and session-registry tests authored in Tasks 1-2 — stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query → boot resolver | `?new=1` is attacker-influenceable (a crafted link) |
| Browser devtools → localStorage | Same-origin scripts/users can read/write session data and registry |
| localStorage quota → write path | Unbounded session growth could exhaust quota |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering / Injection | `history.replaceState` on `?new=1` | mitigate | replaceState receives `window.location.pathname` ONLY — never the query string or any user-controlled value. No open-redirect surface (no `window.open(url)` in this plan). Verified by acceptance criteria grep. |
| T-03-02 | Spoofing | `?new=1` param manipulation by a third party | accept | No server-side effect; only generates a fresh client-side tab id. Worst case is a clean session in the attacker's own tab — benign (RESEARCH.md Security Domain). |
| T-03-03 | Denial of Service | localStorage quota via unbounded session growth | mitigate | LRU cap MAX_SESSIONS=5 (D-09) evicts least-recently-modified sessions on write; QuotaExceeded writes are swallowed with console.warn (existing pattern). |
| T-03-04 | Information Disclosure | Registry/session data readable by same-origin scripts | accept | Pre-existing posture (localStorage was already used). Registry stores only UI metadata (dates, counts, filename) — no secrets, no auth tokens. No change in posture. |
| T-03-05 | Tampering | Legacy migration corrupts/loses existing in-progress work | mitigate | Migration is centralized in tab-identity.ts (single point, Pitfall 2); copies value before removing legacy key; D-06 test asserts no data loss. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH.md Package Legitimacy Audit: zero new packages). No slopcheck checkpoint required. |
</threat_model>

<verification>
- `pnpm --filter @folio-mapper/web test --run` is green (includes new tab-identity, session-registry tests + unchanged store tests).
- `pnpm --filter @folio-mapper/web build` (or `tsc --noEmit` via the web project) succeeds — no type errors from the new modules.
- grep confirms `replaceState` uses `window.location.pathname` only.
</verification>

<success_criteria>
- tab-identity.ts resolves all four boot branches synchronously at module load and migrates legacy data without loss (D-04, D-05, D-06, D-08-support, D-13).
- session-registry.ts enforces LRU cap of 5 by last-modified time and never evicts the current tab (D-09, D-14).
- session-storage.ts exposes an onWrite hook for registry updates without breaking existing usage.
- Both Wave-0 test files exist (authored alongside their modules in Tasks 1-2) and pass; the full web suite is green.
</success_criteria>

<output>
Create `.planning/phases/03-new-session/03-01-SUMMARY.md` when done.
</output>
