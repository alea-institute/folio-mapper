---
quick_id: 260525-bz7
description: Update most-recent LLM models from providers (Google, Anthropic, OpenAI)
date: 2026-05-25
---

# Quick Task 260525-bz7: Update most-recent LLM models

## Objective
Refresh the hardcoded model catalogs/defaults so the three named providers (Anthropic,
OpenAI, Google) expose their current latest models. The central registry was already
fairly current (GPT-5.2, Gemini 3.1, Claude Opus 4.6); this fills the specific gaps and
refreshes the stale `curate_demos.py` defaults.

## Verified current models (web-checked 2026-05-25 — use these IDs verbatim)
- **Anthropic** — latest GA is **Claude Opus 4.7** (`claude-opus-4-7`). Sonnet 4.6
  (`claude-sonnet-4-6`) and Haiku 4.5 (`claude-haiku-4-5`) already present. Gap: add Opus 4.7.
  Source: anthropic.com/news/claude-opus-4-7, docs.anthropic.com models overview.
- **OpenAI** — latest are **GPT-5.5** (`gpt-5.5`, flagship/recommended) and **GPT-5.5 Pro**
  (`gpt-5.5-pro`), released 2026-04-24. Registry stops at gpt-5.2. Gap: add gpt-5.5 + gpt-5.5-pro.
  Source: openai.com/index/introducing-gpt-5-5, platform.openai.com models.
- **Google** — latest GA flagship is **Gemini 3.5 Flash** (`gemini-3.5-flash`); **Gemini 3.1
  Flash-Lite** (`gemini-3.1-flash-lite`) also GA. Registry stops at gemini-3.1-pro-preview.
  Gap: add gemini-3.5-flash + gemini-3.1-flash-lite. Source: blog.google gemini-3-5, cloud.google.com.

## Files to change (4 sync points)
1. `backend/app/services/llm/registry.py`
   - `KNOWN_MODELS[OPENAI]`: append `gpt-5.5`, `gpt-5.5-pro` (ctx 1047576) after gpt-5.2-pro
   - `KNOWN_MODELS[ANTHROPIC]`: append `claude-opus-4-7` (ctx 200000) after claude-opus-4-6
   - `KNOWN_MODELS[GOOGLE]`: append `gemini-3.1-flash-lite`, `gemini-3.5-flash` (ctx 1048576)
   - `DEFAULT_MODELS`: OPENAI `gpt-5.2`→`gpt-5.5`; GOOGLE `gemini-2.5-flash`→`gemini-3.5-flash`;
     GITHUB_MODELS `openai/gpt-5.2`→`openai/gpt-5.5`. ANTHROPIC stays `claude-sonnet-4-6`
     (Sonnet is the correct default tier; no newer Sonnet exists — Opus 4.7 is too costly for a default).
2. `backend/app/services/llm/anthropic_provider.py` — `_FALLBACK_MODELS`: append `claude-opus-4-7`.
3. `packages/core/src/llm/provider-meta.ts` (frontend mirror) — `defaultModel`: OpenAI
   `gpt-5.2`→`gpt-5.5`; Google `gemini-2.5-flash`→`gemini-3.5-flash`; GitHub Models
   `openai/gpt-5.2`→`openai/gpt-5.5`. Anthropic stays.
4. `scripts/curate_demos.py` — `DEFAULT_MODELS` (stale): anthropic
   `claude-3-5-sonnet-latest`→`claude-sonnet-4-6`; openai `gpt-4o`→`gpt-5.5`; google
   `gemini-1.5-pro-latest`→`gemini-3.5-flash`.

## Scope notes
- Focused on the three explicitly-named providers. Mistral/Cohere/Meta/xAI/Groq entries in
  the registry are recent (command-a-reasoning-08-2025, llama-4, grok-4) and left as-is.
- Existing committed demo.json files are untouched (they record the model used at curation
  time); the phase-04 tests assert against those files, not `curate_demos.py` defaults — safe.
- Backend `test_llm.py` ordering test only references gpt-5/gpt-5.2 in its own fixture, and
  the default-presence test only checks each provider HAS a default — both safe with appends
  + value changes. Run `pnpm test:api` + `pnpm --filter @folio-mapper/web test` to confirm.

## Verification
- `python -c "import ast"` parse-check of edited .py; `pnpm --filter @folio-mapper/web build` typecheck for .ts
- `pnpm test:api` (backend pytest) green; `pnpm --filter @folio-mapper/web test` green
